# 开发环境前端配置
# 直连后端：浏览器从 http://localhost:5173 直接打 http://127.0.0.1:8000，
# 由后端 CORS 放行（已在 api/app/core/config.py 默认放行 5173）。
VITE_API_BASE_URL=https://8000.wxneed.com
VITE_API_PREFIX=/api/v1

# 应用元信息
VITE_APP_NAME=Mindmap
VITE_APP_ENV=development
