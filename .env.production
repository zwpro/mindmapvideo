# 生产环境前端配置
# 请替换成你真实的后端域名；前端构建时会把它静态替换进 bundle
# 注意：后端 CORS_ORIGINS 必须包含前端域名，否则浏览器会被拦截
VITE_API_BASE_URL=https://mindmap-api.vuseai.com
VITE_API_PREFIX=/api/v1

# 应用元信息
VITE_APP_NAME=Mindmap
VITE_APP_ENV=production
